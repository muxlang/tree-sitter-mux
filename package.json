{
  "name": "tree-sitter-mux",
  "version": "0.4.1",
  "description": "Mux programming language grammar for Tree-sitter",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/muxlang/tree-sitter-mux"
  },
  "scripts": {
    "sync-matrix": "curl --proto '=https' -fsSL https://raw.githubusercontent.com/muxlang/mux-syntax-highlighting/main/shared/syntax-matrix.json -o /tmp/syntax-matrix-canonical.json && mv /tmp/syntax-matrix-canonical.json ./syntax-matrix.json"
  }
}
